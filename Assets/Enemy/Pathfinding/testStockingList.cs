using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testStockingList : MonoBehaviour
{
    /*public Transform target;
    public Transform target2;
    // Start is called before the first frame update
    public void ListTest(Transform envoyer, List<GameObject> list)
    {
        list.Add(new List<GameObject>());
        list[list.Count-1].Add(this.gameObject);
        StartCoroutine(Wait(list));
        if(target != null)
            target.GetComponent<testStockingList>().ListTest(list);
    }
    IEnumerator Wait(List<List<GameObject>> list)
    {
        yield return new WaitForSeconds(1f);
        Debug.Log(list[list.Count-1].Count);
    }*/
}
